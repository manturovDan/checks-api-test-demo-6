name: GH CI

on: 
  push:
    branches:
          - "*"
    tags:
      - "v*"
jobs:
  simple:
    runs-on: ubuntu-latest
  
    steps:
      - name: README
        id: vars
        run: echo "version=$(pwd)" >> $GITHUB_OUTPUT
